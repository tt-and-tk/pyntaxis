; expect: asm syntax error: fail argument count 'call func func' (expected 'call <function name>' or 'call <register>')
; call引数過多のエラーテスト（異常系）
; callの引数は呼び出し先関数名1つのみ

.global main, func

main:
    call func func
    ret

func:
    ret
