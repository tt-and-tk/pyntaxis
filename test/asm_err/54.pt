; expect: asm syntax error: label '.L0' defined in 'func' referenced from 'main'
; jmpで別の関数に定義された局所ラベルを参照した → エラーになるべき
; 局所ラベルを参照できるのは，参照元と同じ関数内に定義されたラベルだけ

.global main, func

main:
    jmp .L0         ; .L0 はfuncに定義されている
    ret

func:
.L0:
    ret
