; expect: asm syntax error: .global must start at the beginning of the line and be followed by a space '	.global main'
; .globalの前にタブがある → エラーになるべき
; .globalは行頭から書く(タブは非対応)

	.global main

main:
    ret
