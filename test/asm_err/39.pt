; expect: asm syntax error: fail arguments 'call 5' (expected 'call <function name>' or 'call <register>')
; callの呼び出し先に数値を書いた → エラーになるべき
; 呼び出し先は関数名かレジスタのみで，番地の数値を直接指定する書き方は持たない

.global main

main:
    call 5
    ret
